export function renderLines(container, lines, compact=false){container.innerHTML='';lines.forEach((item,index)=>{const line=document.createElement('div');line.className=`line ${item.type}${item.changing?' changing':''}`;line.title=`Лінія ${index+1}: ${item.value}`;if(item.changing){const mark=document.createElement('span');mark.className='change-mark';mark.textContent=item.value===6?'×':'○';line.appendChild(mark)}container.appendChild(line)});if(compact)container.classList.add('history-lines')}
export function showToast(message){const el=document.querySelector('#toast');el.textContent=message;el.classList.add('show');clearTimeout(showToast.timer);showToast.timer=setTimeout(()=>el.classList.remove('show'),2200)}
export function switchTab(name){document.querySelectorAll('.tab-content').forEach(el=>el.classList.toggle('active',el.id===`tab-${name}`));document.querySelectorAll('.nav-btn').forEach(el=>el.classList.toggle('active',el.dataset.tab===name));window.scrollTo({top:0,behavior:'smooth'})}
